node_modules
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
uploads/broadcast
uploads/operator-logos

# ── Log files — may contain URL paths, IPs, or PII ────────────────────────
*.log
logs/
attached_assets/*.txt

# ── Secrets — ne JAMAIS commiter ──────────────────────
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
secrets/
config/secrets*
